// Declarando variáveis
// Declarei os valores de cada uma
let a,b,c
a=1
b=2
c=3
// Neste exemplo tudo pode ser feito na mesma linha usando ( ; )
let d,e,f; d=1; e=2; f=3;
// Na declaração de variaveis usa usasse três palavras chave são elas: var, let, const. o let não pode ser redeclarado;
// Lembrando que o const precisa ser inicioalizado e não pode ser mudado ele é constante
var exemplo;
let exemplo2;
const exemplo3=1;
// As variaveis não podem começar com números, nem aver espaços entre as palavras, recomendo o uso do CAMALLCASE.

var nome, sobrenome, nomeCompleto, idade;
idade = 30;
nome = "João";
sobrenome = "Da Silva";
nomeCompleto = nome +" "+ sobrenome;
document.getElementById("texto").innerHTML = nomeCompleto